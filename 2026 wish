<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Happy New Year 2026</title>
    <style>
        body {
            margin: 0;
            background: linear-gradient(to bottom, #000428, #004e92);
            color: #fff;
            font-family: sans-serif;
            text-align: center;
            display: flex;
            justify-content: center;
            align-items: center;
            height: 100vh;
        }
        .card {
            background: rgba(255, 255, 255, 0.1);
            padding: 30px;
            border-radius: 15px;
            border: 2px solid #ffd700;
            width: 85%;
            max-width: 400px;
        }
        h1 { color: #ffd700; }
        .odia { color: #00ffcc; font-size: 1.2rem; margin: 10px 0; }
        .name { font-size: 2rem; font-weight: bold; color: #ff4d4d; }
        input { padding: 10px; width: 80%; margin: 20px 0 10px; border-radius: 5px; border: none; }
        button { padding: 10px 20px; background: #28a745; color: white; border: none; border-radius: 5px; cursor: pointer; }
        #link-box { display: none; margin-top: 15px; background: #000; padding: 10px; font-size: 0.8rem; word-break: break-all; }
        .creator { margin-top: 20px; font-size: 0.8rem; color: #aaa; }
    </style>
</head>
<body>
    <div class="card">
        <h1 id="wish">Happy New Year 2026!</h1>
        <p class="odia">ନୂତନ ବର୍ଷର ଅନେକ ଅନେକ ଶୁଭକାମନା!</p>
        <p class="name" id="display-name">Biswajit</p>
        
        <input type="text" id="name-in" placeholder="Enter your name">
        <button onclick="make()">Create Link</button>
        <div id="link-box"></div>

        <div class="creator">Created by <b>Biswajit</b></div>
    </div>

    <script>
        const urlParams = new URLSearchParams(window.location.search);
        const n = urlParams.get('n');
        if (n) {
            document.getElementById('display-name').innerText = n;
            document.getElementById('wish').innerText = n + " is wishing you!";
        }

        function make() {
            const val = document.getElementById('name-in').value;
            if (val) {
                const link = window.location.href.split('?')[0] + "?n=" + encodeURIComponent(val);
                const box = document.getElementById('link-box');
                box.style.display = "block";
                box.innerText = link;
                alert("Link created! Copy it from the black box.");
            }
        }
    </script>
</body>
</html>
